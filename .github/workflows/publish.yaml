name: "publish"

on:
  push:
    branches:
      - release

jobs:
  publish-tauri:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          # Apple Silicon (Native arm64 build)
          - platform: "macos-latest"
            args: "--target aarch64-apple-darwin"
          # Intel (Native x86_64 build)
          - platform: "macos-13"
            args: "--target x86_64-apple-darwin"
          # Linux
          - platform: "ubuntu-22.04"
            args: ""
          # Windows
          - platform: "windows-latest"
            args: ""

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: setup bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ startsWith(matrix.platform, 'macos') && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      # Fix for macOS: Install pkg-config and libpng locally via homebrew
      - name: install dependencies (macOS)
        if: startsWith(matrix.platform, 'macos')
        run: |
          brew install libpng pkg-config

      # Fix for Windows: Install pkg-config and libpng via vcpkg
      - name: install dependencies (Windows)
        if: matrix.platform == 'windows-latest'
        run: |
          vcpkg install libpng:x64-windows
          echo "VCPKG_ROOT=$env:VCPKG_INSTALLATION_ROOT" >> $env:GITHUB_ENV

      - name: install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.0-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libpng-dev pkg-config

      - name: install frontend dependencies
        run: bun install --frozen-lockfile

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # REMOVED LIBPNG_NO_PKG_CONFIG: 1 to allow pkg-config to naturally discover system libraries
        with:
          tagName: app-v__VERSION__
          releaseName: "App v__VERSION__"
          releaseBody: "See the assets to download this version and install."
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
